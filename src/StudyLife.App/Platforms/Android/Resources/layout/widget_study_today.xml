<?xml version="1.0" encoding="utf-8"?>
<!-- RemoteViews layout of the study-progress widget. A single Chronometer for both
     live cases (countdown while a focus timer is running, counting up the day's study
     time while a study session is running) - visibility and mode are set by
     StudyTodayWidgetProvider. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#6b6965"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="✦ Heute" />

    <Chronometer
        android:id="@+id/widget_chrono"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:textSize="30sp"
        android:textColor="#CC785C"
        android:fontFamily="sans-serif-light" />

    <TextView
        android:id="@+id/widget_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30sp"
        android:textColor="#CC785C"
        android:fontFamily="sans-serif-light"
        android:maxLines="1"
        android:text="0 min" />

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#6b6965"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="gelernt" />

    <TextView
        android:id="@+id/widget_footer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textSize="12sp"
        android:textColor="#e8e6e0"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
