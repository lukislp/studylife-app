import SwiftUI

// Placeholder app - exists only so xcodebuild can generate profiles for app.studylife.mobile
// (see project.yml).
@main
struct ShellApp: App {
    var body: some Scene {
        WindowGroup { Text("StudyLife Provisioning Shell") }
    }
}
